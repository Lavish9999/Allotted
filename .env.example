# Allotted cloud configuration (Phase 2)
# Copy to .env for local tooling/CI. NEVER commit real values.
# The iOS app itself reads www/cloud-config.js (see www/cloud-config.example.js),
# because a static Capacitor web app has no runtime env injection.
SUPABASE_URL=
SUPABASE_ANON_KEY=
